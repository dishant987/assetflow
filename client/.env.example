# Leave empty in development (Vite proxy will forward /api to localhost:3001).
# In production, set this to your Render backend URL, including /api.
# Example: https://assetflow-backend.onrender.com/api
VITE_API_URL=
